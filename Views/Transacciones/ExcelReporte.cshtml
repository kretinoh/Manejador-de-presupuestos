@{
    ViewData["Title"] = "Reporte Excel";
    var hoy = DateTime.Today;
    var haceUnMes = hoy.AddMonths(-1);
    var mesActual = hoy.Month;
    var annyoActual = hoy.Year;
    var mesAnterior = haceUnMes.Month;
    var mesAnteriorAnnyo = haceUnMes.Year;
    var annyoAnterior = annyoActual -1;
}

<partial name="_Submenu" Model="SubMenuTransacciones.Excel" />

<h1>Exportar transacciones a Excel</h1>
<div class="mb-3">
    <a asp-action="ExportarExcelPorMes" asp-route-mes="@mesActual" asp-route-annyo="@annyoActual" class="btn btn-success">
        Exportar Mes actual
    </a> ~@hoy.ToString("MMM yyyy")
</div>

<div class="mb-3">
    <a asp-action="ExportarExcelPorMes" asp-route-mes="@mesAnterior" asp-route-annyo="@mesAnteriorAnnyo" class="btn btn-success">
        Exportar Mes anterior
    </a> ~@haceUnMes.ToString("MMM yyyy")
</div>

<div class="mb-3">
    <a asp-action="ExportarExcelPorAnnyo"asp-route-annyo="@annyoActual" class="btn btn-success">
        Exportar Año actual
    </a> ~@hoy.ToString("yyyy")
</div>

<div class="mb-3">
    <a asp-action="ExportarExcelPorAnnyo" asp-route-annyo="@annyoAnterior" class="btn btn-success">
        Exportar Año anterior
    </a> ~@annyoAnterior
</div>

<div class="mb-3">
    <a asp-action="ExportarExcelTodo" class="btn btn-success">
        Exportar Todo
    </a>
</div>
